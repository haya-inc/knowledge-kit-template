---
description: inbox/ → source/ の同期フローを実行 (sync スキル)
argument-hint: "[任意のメモ]"
---

knowledge-kit の `sync` スキル (`.claude/skills/sync/SKILL.md`) を実行してください。

- scan → 変換 → record → prune → verify → update-readme → dashboard の
  順に進める
- 途中で失敗したら完了扱いにせず、何が未解決かを報告する
- 補足メモ: $ARGUMENTS
